# First release

Build one Next.js application directly in the saved checkout. Scope: email/password accounts, public maker profiles, multiple SaaS profiles per owner, image uploads, discovery, category filters, newest listings, and a USD MRR leaderboard.

1. Inspect the existing Supabase project and pin compatible stable dependencies.
2. Implement versioned schema, owner RLS, private metric history, and public metric projection.
3. Build responsive public pages and authenticated editing flows.
4. Run lint, types, build, domain tests, database access tests, and browser checks.
5. Document configuration, verified behavior, and remaining account setup. Show a local preview.

Excluded: company accounts, sales, escrow, deal rooms, messages, and advanced revenue verification. No external deployment or Git history changes.
