// The operator named in the privacy policy as the party responsible for personal data. Fill in the
// legal name and a contact address for privacy requests before the site opens to the public;
// until then the policy says it is a draft.
export const operator = null as { name: string; email: string } | null;

export const privacyUpdated = "September 25, 2026";
