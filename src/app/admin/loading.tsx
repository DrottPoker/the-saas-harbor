import { PageSkeleton } from "@/components/page-skeleton";

// The admin layout draws the Shell and the admin navigation around it.
export default function Loading() {
  return <PageSkeleton inShell />;
}
