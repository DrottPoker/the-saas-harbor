@AGENTS.md

## Git

- Commit to the current branch after each completed task, once the relevant checks pass. This overrides the global rule that commits need an explicit request.
- Pushing, switching or creating branches, and rewriting history still need explicit approval.
